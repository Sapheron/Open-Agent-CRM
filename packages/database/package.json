{
  "name": "@wacrm/database",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "prisma generate && tsc",
    "db:generate": "prisma generate",
    "db:migrate": "bash -c 'if [ -z \"$DATABASE_URL\" ]; then echo \"ERROR: DATABASE_URL not set\"; exit 1; fi; prisma migrate dev'",
    "db:migrate:deploy": "bash -c 'if [ -z \"$DATABASE_URL\" ]; then echo \"ERROR: DATABASE_URL not set\"; exit 1; fi; prisma migrate deploy'",
    "db:push": "bash -c 'if [ -z \"$DATABASE_URL\" ]; then echo \"ERROR: DATABASE_URL not set\"; exit 1; fi; prisma db push'",
    "db:seed": "bash -c 'if [ -z \"$DATABASE_URL\" ]; then echo \"ERROR: DATABASE_URL not set\"; exit 1; fi; tsx prisma/seed.ts'",
    "db:studio": "prisma studio",
    "db:reset": "bash -c 'if [ -z \"$DATABASE_URL\" ]; then echo \"ERROR: DATABASE_URL not set\"; exit 1; fi; prisma migrate reset --force'"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.7.0",
    "@prisma/client": "^7.7.0",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^22.0.0",
    "@types/pg": "^8.20.0",
    "bcryptjs": "^2.4.3",
    "prisma": "^7.7.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2"
  }
}
